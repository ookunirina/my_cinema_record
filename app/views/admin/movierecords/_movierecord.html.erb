<tr>
  <td>
    <%= movierecord.id %>
  </td>
  <td>
    <%= link_to movierecord.title, admin_movierecord_path(movierecord) %>
  </td>
  <td>
    <%= movierecord.user.name %>
  </td>
  <td>
    <%= l movierecord.created_at, format: :long %>
  </td>
  <td>
    <%= link_to t('defaults.show'), admin_movierecord_path(movierecord), class: 'btn btn-info' %>
    <%= link_to t('defaults.edit'), edit_admin_movierecord_path(movierecord), class: 'btn btn-success' %>
    <%= button_to t('defaults.delete'), admin_movierecord_path(movierecord), id: "button-delete-#{movierecord.id}", method: :delete, data: { turbo: false }, form: { onSubmit: "return check();" }, class: 'btn btn-danger'%>
    </td>
</tr>

<script>
  function check() {
    if (window.confirm('本当に削除しますか？')) { 
      return true;
    } else {
      window.alert('キャンセルされました'); 
      return false; 
    }
  }
</script>